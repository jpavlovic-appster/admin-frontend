import { SentancecasePipe } from './sentancecase.pipe';

describe('SentancecasePipe', () => {
  it('create an instance', () => {
    const pipe = new SentancecasePipe();
    expect(pipe).toBeTruthy();
  });
});
